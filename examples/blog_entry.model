import: user
name: blog_entry
backend: mnesia(
                schema = erl_db,
                table = my_blog_entry
                )


fields:
    id      :: primary_key(auto_increment)
    title   :: string(max_length = 255, index)
    author  :: one_to_many(user.id)
    created :: datetime()
    text    :: string()
